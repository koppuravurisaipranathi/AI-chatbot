# Generative AI Mental Health Chatbot

## 💡 Description
This chatbot offers empathetic, AI-driven mental health support using OpenAI's GPT model via Flask.

## 🚀 Setup Instructions

### Backend
1. Navigate to backend folder:
    ```bash
    cd backend
    pip install -r requirements.txt
    ```
2. Add your OpenAI key in `.env` file.
3. Run the server:
    ```bash
    python app.py
    ```

### Frontend
Open `frontend/index.html` in a browser.

## ⚠️ Disclaimer
This tool is **not a substitute for professional mental health care**.
